dependencies {
    // Hexagonale Architektur: Die Domain hat KEINE Abhängigkeiten zu anderen Modulen!

    // Nützliche Helfer für reine Java-Logik sind erlaubt:
    implementation("com.github.f4b6a3:tsid-creator:5.2.6") // Für Business Keys
}