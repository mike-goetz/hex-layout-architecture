version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: travel_postgres
    environment:
      POSTGRES_DB: travel_db
      POSTGRES_USER: travel_user
      POSTGRES_PASSWORD: travel_password
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U travel_user -d travel_db" ]
      interval: 5s
      timeout: 5s
      retries: 5

#  travel-core:
#    build:
#      context: .
#      dockerfile: travel-core/Dockerfile
#    container_name: travel_core_service
#    ports:
#      - "8080:8080"
#    environment:
#      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/travel_db
#      SPRING_DATASOURCE_USERNAME: travel_user
#      SPRING_DATASOURCE_PASSWORD: travel_password
#    depends_on:
#      postgres:
#        condition: service_healthy

volumes:
  pgdata: